
include $(SRC_DIRECTORY)/Volume_io/Makefile.include

BIC_PL_DIRECTORY  = $(SRC_DIRECTORY)/BIC_PL
BIC_PL_INCLUDE    = -I$(BIC_PL_DIRECTORY)/Include $(VOLUME_IO_INCLUDE) \
                    $(RECIPES_INCLUDE)

BIC_PL_LIBS = -L$(BIC_PL_DIRECTORY) -lbicpl $(VOLUME_IO_LIBS)
BIC_PL_LIBS-O3 = -L$(BIC_PL_DIRECTORY) -lbicpl-O3 $(VOLUME_IO_LIBS-O3)

BIC_PL_LINTLIBS = $(BIC_PL_DIRECTORY)/llib-lbicpl.ln $(VOLUME_IO_LINT_LIBS)
